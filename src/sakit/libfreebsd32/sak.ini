# Config file for freeBSD32
BINESPEAK=speak_x86                # speak or espeak binary name.
LIBESPEAK=                         # espeak library name (needed for espeak binary).
DIRESPEAKDATA=../                   # directory of espeak-data ( ./ or / => calling dir, ../ => parent dir).
LIBPORTAUDIO=libportaudio_x86.so   # portaudio library name.

